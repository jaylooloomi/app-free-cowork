{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the palette, wizard and settings windows",
  "windows": ["palette", "wizard", "settings"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
